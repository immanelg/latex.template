# LaTeX base template for Simple LaTeX documents

Uses latexmk and watchexec.
Includes basic template.tex and ./do build script.
Available commands:
```sh
./do build
./do watch
./do open
./do clean
./do archive
```
